# Copy to .env and fill in your values.
#   cp env.example .env

# ---------------------------------------------------------------------------
# LLM Provider: openai | anthropic | gemini | grok
# ---------------------------------------------------------------------------
LLM_PROVIDER=openai

# API key for the chat provider (required).
LLM_API_KEY=sk-your-api-key

# For docker-compose (all four bots), set provider-specific keys:
# OPENAI_API_KEY=sk-...           (ChatGPT bot + image rendering for Claude)
# ANTHROPIC_API_KEY=sk-ant-...    (Claude bot)
# GEMINI_API_KEY=AI...            (Gemini bot + native Imagen image gen)
# GROK_API_KEY=xai-...            (Grok bot + native image gen)

# Optional: override the default chat model for the chosen provider.
# LLM_CHAT_MODEL=gpt-5.2-2025-12-11
# LLM_BASE_URL=

# ---------------------------------------------------------------------------
# Image generation
# ---------------------------------------------------------------------------
# Each provider handles images differently:
#   openai  → DALL-E 3 (native)
#   grok    → grok-2-image (native)
#   gemini  → Imagen 3 (native Google API, base64 → uploaded to image-store)
#   anthropic → Claude refines the prompt with its own creative voice,
#               then delegates rendering to IMAGE_PROVIDER (default: openai)
#
# For anthropic, set IMAGE_PROVIDER + IMAGE_API_KEY for the renderer:
# IMAGE_PROVIDER=openai
# IMAGE_API_KEY=sk-your-openai-key
#
# Optional: override image model
# IMAGE_MODEL=dall-e-3
# IMAGE_BASE_URL=

# Bot identity
BOT_ADDRESS=ut1_bot_chatgpt_default
BOT_NAME=chatgpt_bot

# Server
NODE_URL=http://localhost:8000
CIS_APP_PUBKEY=ut1_cis_demo_pubkey

# Timing
POLL_INTERVAL_S=10
VOTE_RECONSIDER_S=3600

# Set to "false" to skip image generation entirely
ENABLE_IMAGES=true

# Image store — required for Gemini (base64 output) and for re-hosting
# temporary URLs from other providers.
IMAGE_STORE_URL=http://localhost:8001

# ---------------------------------------------------------------------------
# Web search (optional) — Brave Search API
# ---------------------------------------------------------------------------
# Free tier: 2,000 queries/month — https://brave.com/search/api/
# Before voting or suggesting options, bots run a multi-step research
# loop: search → read articles → follow up → summarize. The LLM drives
# the loop, deciding what to search and which pages to read.
# Leave blank to disable.
# SEARCH_API_KEY=BSA...
#
# Max research steps per decision (search/read calls). Default: 6.
# Lower = faster/cheaper, higher = more thorough.
# MAX_RESEARCH_STEPS=6